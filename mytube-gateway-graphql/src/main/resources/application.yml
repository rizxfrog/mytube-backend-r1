server:
  port: 8080
spring:
  graphql:
    schema:
      locations: classpath:/graphql/
  config:
    import: optional:nacos:application.yml
  application:
    name: mytube-gateway-graphql
  data:
    redis:
      host: localhost
      port: 6379
      password: redis_fQHPzE
#  cloud:
#    nacos:
#      config:
#        import-check:
#          enabled: false
#      server-addr: www.serein.com:8848
#      username: nacos
#      password: nacos
dubbo:
  application:
    name: mytube-gateway-graphql
    qos-port: 22220
  protocol:
    name: dubbo
    port: -1
  consumer:
    check: false
  registry:
    address: nacos://localhost:8848
    parameters:
      enable-empty-protection: true
      register-consumer-url: true
    username: nacos
    password: nacos
