server:
  port: 8106
spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
    default-schema: public
    schemas: public
    table: mytube_im_flyway_history
  config:
    import: optional:nacos:application.yml
  application:
    name: mytube-im-service
  datasource:
    url: jdbc:postgresql://www.serein.com:5432/mytube
    username: user_D27Hrf
    password: password_Rh7CzX
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: www.serein.com
      port: 6379
      password: redis_6KtBtd
  cloud:
    nacos:
      config:
        import-check:
          enabled: false
      server-addr: www.serein.com:8848
      username: nacos
      password: nacos
dubbo:
  application:
    name: mytube-im-service
  registry:
    address: nacos://${spring.cloud.nacos.server-addr}?username=nacos&password=nacos
  protocol:
    name: dubbo
    port: -1
